# URL of the deployed FastAPI backend (Render), no trailing slash.
# For local dev against `uvicorn app.main:app --reload`, use http://localhost:8000
VITE_API_URL=https://exam-hall-allocator-backend.onrender.com
